
export default function LoginLayout({ children }) {
  return (
    <html lang="ja">
      <body className="min-h-screen bg-gradient-to-br from-sky-100 to-green-100 flex items-center justify-center">
        {children}
      </body>
    </html>
  );
}
